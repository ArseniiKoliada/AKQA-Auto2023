# Pytest markers. Use pytest -m {marker_name} to execute tests with corresponding mark
[pytest]
markers = 
    change: Tests that modify name of a user
    check: Tests that check users name 
    http: Tests that check HTTP
    api: Tests for GitHub API
    database: Database Tests
    ui: UI tests with Selenium